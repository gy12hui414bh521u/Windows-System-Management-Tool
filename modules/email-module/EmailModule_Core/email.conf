# SMTP 基本配置
server_ip=127.0.0.1
port=2525
from_address=test@example.com

# Level 3：SMTP 认证设置（目前对本地 smtp4dev 可保持关闭）
use_auth=0
username=
password=

# 网络相关配置
io_timeout_ms=5000   # 读写超时 5 秒，0 或不写表示默认
max_retry=2          # 最多尝试 2 次发送